/**
 * Brand mark slot next to the nav collapse control.
 *
 * Intentionally renders nothing: the EPL logo already lives on the nav bar via
 * NavBrand (admin.components.beforeNav), so showing it here too would duplicate
 * it beside the collapse toggle. Wired via admin.components.graphics.Icon.
 */
export function Icon() {
  return null
}
