/** Default EPL Ghana logo (public path), used when no CMS upload is set */
export const EPL_LOGO_SRC = '/assets/cropped-EPL-Ghana-logo.png'

/** White logo for dark backgrounds (email header, etc.) */
export const EPL_LOGO_WHITE_SRC = '/assets/img/logo-white.svg'

/** User-facing name for the Payload admin */
export const CMS_NAME = 'Content Management System'

/** Full branded product name (page titles, MFA issuer, etc.) */
export const CMS_PRODUCT_NAME = `EPL Ghana ${CMS_NAME}`
